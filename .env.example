# FlowSignal 環境変数のサンプル。`.env` にコピーして値を設定する。
# .env は .gitignore 済み（コミットしないこと）。

# --- J-Quants API（任意。未設定なら yfinance のみで動作） ---
# https://jpx-jquants.com/ で取得したログイン情報
JQUANTS_MAIL_ADDRESS=
JQUANTS_PASSWORD=

# --- Claude API（M3 のニュース分析で使用。M1 では不要） ---
ANTHROPIC_API_KEY=
